
We start with data for modeling, learn how to specify and train models with different engines using the **mt_data** and understand why these functions are designed this way.

As a first step in modeling, it’s always a good idea to plot the data, along with the plots we provide a comprehensive metrics for descriptive statistics. 

Similar with Tidy model when you using **mt_model** includes three ceng, model type, model mode, and model engine. 
model type is a tongyong and user friendly interface 
a method for _fitting_ or training the model, the model **engine**